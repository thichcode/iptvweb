name: Release APK
on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      version:
        description: 'Custom tag (e.g. v1.0.0). Default: auto'
        required: false
permissions: { contents: write }
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: 'temurin', java-version: '21' }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm }
      - uses: android-actions/setup-android@v3
      - run: npm ci
      - run: npm run build
      - name: Debug info
        run: |
          echo "ANDROID_HOME=$ANDROID_HOME"
          echo "JAVA_HOME=$JAVA_HOME"
          ls -la node_modules/.bin/cap node_modules/.bin/capacitor 2>/dev/null || echo "cap not found in .bin"
          node -e "try{require.resolve('@capacitor/cli');console.log('CLI found')}catch(e){console.log('CLI NOT found:'+e.message)}"
      - name: Add Android platform
        run: node node_modules/.bin/cap add android
      - name: Sync web to Android
        run: node node_modules/.bin/cap sync android
      - name: Install custom app icon
        run: |
          ICON="assets/app-icon.png"
          node scripts/make-banner.cjs
          if [ -f "$ICON" ]; then
            for D in android/app/src/main/res/mipmap-*dpi; do
              [ -d "$D" ] || continue
              cp "$ICON" "$D/ic_launcher.png"
              cp "$ICON" "$D/ic_launcher_round.png"
              cp "$ICON" "$D/ic_launcher_foreground.png"
            done
            mkdir -p android/app/src/main/res/drawable
            cp assets/banner.png android/app/src/main/res/drawable/banner.png
          fi
      - name: Create TV banner drawable
        run: |
          mkdir -p android/app/src/main/res/drawable
          printf '<?xml version="1.0" encoding="utf-8"?>\n<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">\n<solid android:color="#FF0D0D0D"/>\n</shape>\n' > android/app/src/main/res/drawable/tv_banner.xml
      - name: Patch AndroidManifest for TV
        run: |
          M="android/app/src/main/AndroidManifest.xml"
          if [ -f "$M" ]; then
            # Add TV features before </manifest>
            sed -i 's|</manifest>|  <uses-feature android:name="android.hardware.touchscreen" android:required="false" />\n  <uses-feature android:name="android.software.leanback" android:required="false" />\n  <uses-feature android:name="android.hardware.screen.landscape" android:required="false" />\n</manifest>|' "$M"
            # Add banner to application tag
            sed -i 's|android:supportsRtl="true"|android:supportsRtl="true"\n        android:banner="@drawable/tv_banner"|' "$M"
            # Add resizeable and TV launcher to activity
            sed -i 's|android:exported="true"|android:exported="true"\n            android:resizeableActivity="true"|' "$M"
            sed -i 's|<category android:name="android.intent.category.LAUNCHER" />|<category android:name="android.intent.category.LAUNCHER" />\n                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />|' "$M"
          fi
      - name: Use stable debug keystore
        run: |
          mkdir -p ~/.android
          cp certs/debug.keystore ~/.android/debug.keystore
          mkdir -p android/app
          cp certs/debug.keystore android/app/debug.keystore
      - name: Build APK
        run: cd android && chmod +x gradlew && ./gradlew assembleDebug --no-daemon
      - name: Rename APK
        run: cp android/app/build/outputs/apk/debug/app-debug.apk WebPhim.apk
      - name: Set tag
        id: tag
        run: |
          TAG="${{ github.event.inputs.version }}"
          if [ -z "$TAG" ]; then TAG="build-${{ github.run_number }}"; fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          files: WebPhim.apk
          generate_release_notes: true
          make_latest: true
